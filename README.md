# BetterClaw Installer

One-click install:

```bash
curl -sL https://raw.githubusercontent.com/devvcore/betterclaw-install/main/install.sh | bash
```

Then run `claw init` to set up.

Requires Node.js 20+.
